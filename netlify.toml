[build]
  publish = "."
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[functions."parse-invoice"]
  timeout = 26

[functions."ai-pricing"]
  timeout = 26

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
